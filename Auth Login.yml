info:
  name: Auth Login
  type: http
  seq: 2

http:
  method: POST
  url: https://musicdjam.kabada.org/api/auth/login/
  body:
    type: json
    data: |-
      {
        "username": "example",
        "password": "password4231"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
